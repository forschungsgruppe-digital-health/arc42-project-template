# 5. Building Block View

[Back to the architecture docs index](README.md)

> **In brief (for newcomers):** how the system decomposes into parts — the static structure, top-down. Terms are defined in the [glossary](12_glossary.md).

> ⚠️ HUMAN INPUT REQUIRED — Level 1 (whole system) and level 2 whiteboxes. This is the most code-derivable section: top-level modules/apps/services from the build and directory layout. Each block gets a Mermaid diagram AND a description table (component, responsibility, interfaces, technology).

<!-- Expert detail goes below the newcomer lede: fill with precise, verifiable content
     (exact names, versions, values), tables, and Mermaid diagrams (no colors/styles).
     Keep this section consistent with the code and with the other arc42 sections. -->
