# 7. Deployment View

[Back to the architecture docs index](README.md)

> **In brief (for newcomers):** where the software runs — infrastructure, containers, networks, ports. Terms are defined in the [glossary](12_glossary.md).

> ⚠️ HUMAN INPUT REQUIRED — A deployment diagram. Derivable from docker-compose / Dockerfiles / k8s manifests: services, ports, networks, volumes, dependencies.

<!-- Expert detail goes below the newcomer lede: fill with precise, verifiable content
     (exact names, versions, values), tables, and Mermaid diagrams (no colors/styles).
     Keep this section consistent with the code and with the other arc42 sections. -->
